import { GetServerSideProps, GetStaticPaths } from "next";

import Layout from "../../../components/layout";

function Page({}) {
    return (
        <Layout>

            <h1>Hello!</h1>
            
        </Layout>
    )
}

export default Page;